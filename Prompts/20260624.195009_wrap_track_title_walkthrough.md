# User Prompt
In the 'Tracks List' screen, if the title is longer, it should wrap around the card.  Even if the card heught increases, it is ok

# Walkthrough
I have modified the layout file to allow the track title in the tracks list screen to wrap to multiple lines.

## Changes Made
- Modified [item_track.xml](file:///x:/Projects_X/0_Active/8_Android_APK/01_GPS/GPS_Location_Logger_APK_GITHUB/app/src/main/res/layout/item_track.xml) to remove `android:singleLine="true"` and `android:ellipsize="middle"` from `tvTrackName` TextView.

## Verification
- Checked that layout XML format is correct.
- Gradle build was requested, but was bypassed as permissions were denied.
