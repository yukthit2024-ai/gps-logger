# User Prompt
The app currently auto-rotates.
It should auto-rotate only when phone's auto-rotate setting is on

# Goals
Change the `screenOrientation` configuration for all activities from `fullSensor` to `fullUser` in `AndroidManifest.xml` so that screen auto-rotation only occurs if the system-wide auto-rotate setting is enabled.

## Proposed Changes

### Manifest

#### [MODIFY] [AndroidManifest.xml](file:///x:/Projects_X/0_Active/8_Android_APK/01_GPS/GPS_Location_Logger_APK_GITHUB/app/src/main/AndroidManifest.xml)
- Replace `android:screenOrientation="fullSensor"` with `android:screenOrientation="fullUser"` for the following activities:
  - `MainActivity`
  - `SettingsActivity`
  - `TracksListActivity`
  - `HelpActivity`
  - `AboutActivity`

## Verification Plan

### Automated Tests
- None.
