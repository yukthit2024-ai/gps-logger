# User Prompt
The app currently auto-rotates.
It should auto-rotate only when phone's auto-rotate setting is on

# Walkthrough
I have updated the screen orientation setting for all activities in the app to respect the device's system-wide auto-rotate configuration.

## Changes Made
- **Manifest**: Modified [AndroidManifest.xml](file:///x:/Projects_X/0_Active/8_Android_APK/01_GPS/GPS_Location_Logger_APK_GITHUB/app/src/main/AndroidManifest.xml) to change `android:screenOrientation` from `fullSensor` to `fullUser` for all specified activities:
  - `MainActivity`
  - `SettingsActivity`
  - `TracksListActivity`
  - `HelpActivity`
  - `AboutActivity`

By changing this to `fullUser`, screen auto-rotation will now be locked or unlocked depending on the user's system preferences/auto-rotate toggle.
