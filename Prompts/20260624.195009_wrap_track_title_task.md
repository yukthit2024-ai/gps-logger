# User Prompt
In the 'Tracks List' screen, if the title is longer, it should wrap around the card.  Even if the card heught increases, it is ok

# Tasks
- [x] Remove `singleLine` and `ellipsize` properties from `tvTrackName` in `item_track.xml` to allow wrapping.
- [x] Run Gradle build to verify compilation (Bypassed since build permission was denied, edit verified manually).
