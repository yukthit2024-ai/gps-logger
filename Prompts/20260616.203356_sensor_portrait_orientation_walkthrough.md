# User Prompt
when the mobile phone is tilted upside down, the app has to also turn aound and become vertical again, like google maps does

# Changes

## Android Manifest

- Updated [AndroidManifest.xml](file:///x:/Projects_X/0_Active/8_Android_APK/01_GPS/GPS_Location_Logger_APK_GITHUB/app/src/main/AndroidManifest.xml) to change screen orientation to `"fullSensor"` for all activities that were locked to portrait.
  - Activities updated:
    1. `MainActivity`
    2. `SettingsActivity`
    3. `TracksListActivity`
    4. `HelpActivity`
    5. `AboutActivity`

By using `fullSensor`, the app matches Google Maps' orientation behavior, enabling full 360-degree rotation (including normal portrait, reverse portrait, landscape, and reverse landscape orientations) driven by the physical device sensor.

# Verification Results

## Manual Verification
- Verified that the manifest compiles and contains the correct configuration changes.
- Since this is an Android application, manual orientation testing can be performed on a real device or emulator by rotating the device in all directions.
