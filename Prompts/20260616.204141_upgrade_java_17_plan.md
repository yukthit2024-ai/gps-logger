# User Prompt
upgrade java to 17 or 19 or 21

# Goals
Upgrade Java compiler compatibility from Java 11 to Java 17 (`VERSION_17`) in `app/build.gradle`. Java 17 is the recommended standard LTS version for Android projects targeting API 34.

## Proposed Changes

### Build Configuration

#### [MODIFY] [build.gradle](file:///x:/Projects_X/0_Active/8_Android_APK/01_GPS/GPS_Location_Logger_APK_GITHUB/app/build.gradle)
Change `sourceCompatibility` and `targetCompatibility` inside `compileOptions` to `JavaVersion.VERSION_17`.

```groovy
    compileOptions {
        // java.time (Instant, DateTimeFormatter) requires desugaring below API 26 OR minSdk 26+
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
```

## Verification Plan

### Automated Tests
- Build/compile the project to verify that it compiles successfully under Java 17.
