# User Prompt
on each card, thereis no need for an icon on the left.  A small icon at the begining of title text is sufficinet to indicate if it is a point or a track

# Goals
Remove the left `ImageView` (`ivTrackIcon`) from each track card in the tracks list screen, and remove corresponding references to it in the adapter.

## Proposed Changes

### Layout

#### [MODIFY] [item_track.xml](file:///x:/Projects_X/0_Active/8_Android_APK/01_GPS/GPS_Location_Logger_APK_GITHUB/app/src/main/res/layout/item_track.xml)
- Remove the `ivTrackIcon` ImageView.
- Remove `android:layout_marginStart="16dp"` (or change it to 0dp) from the vertical `LinearLayout` container.

### Adapter

#### [MODIFY] [TrackAdapter.java](file:///x:/Projects_X/0_Active/8_Android_APK/01_GPS/GPS_Location_Logger_APK_GITHUB/app/src/main/java/com/example/gpslocationlogger/TrackAdapter.java)
- Remove `holder.ivTrackIcon.setImageResource(...)` calls.
- Remove `ImageView ivTrackIcon` declaration from `TrackViewHolder` class.
- Remove `ivTrackIcon = itemView.findViewById(R.id.ivTrackIcon);` initialization.

## Verification Plan

### Automated Tests
- Build/compile the project to verify that it compiles successfully without any syntax errors.
