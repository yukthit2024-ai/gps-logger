# User Prompt
In the 'Tracks List' screen, if the title is longer, it should wrap around the card.  Even if the card heught increases, it is ok

# Goals
Allow the track title (`tvTrackName`) in the tracks list screen to wrap to multiple lines instead of being truncated to a single line.

## Proposed Changes

### Layout

#### [MODIFY] [item_track.xml](file:///x:/Projects_X/0_Active/8_Android_APK/01_GPS/GPS_Location_Logger_APK_GITHUB/app/src/main/res/layout/item_track.xml)
Modify `tvTrackName` TextView:
- Remove `android:singleLine="true"`
- Remove `android:ellipsize="middle"`

```xml
            <TextView
                android:id="@+id/tvTrackName"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="My Trip Name"
                android:textColor="#1A1A2E"
                android:textSize="16sp"
                android:textStyle="bold" />
```

## Verification Plan

### Automated Tests
- Run Gradle assembly task to ensure that the layout modification compiles successfully:
  `./gradlew assembleDebug`
