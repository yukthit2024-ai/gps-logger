# User Prompt
when the mobile phone is tilted upside down, the app has to also turn aound and become vertical again, like google maps does

# Tasks
- [x] Modify `AndroidManifest.xml` to replace `"sensorPortrait"` with `"fullSensor"` for the 5 target activities.
- [x] Verify that there are no remaining locked portrait orientations in the manifest.
